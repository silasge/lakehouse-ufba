--- Socioeconomic tables ---

CREATE TABLE IF NOT EXISTS ufba_bronze.vestibular_2005_inscritos (
    ano INTEGER,
    inscrica VARCHAR,
    faixaet VARCHAR,
    idade INTEGER,
    nascim VARCHAR,
    sexo VARCHAR,
    deficie VARCHAR,
    area VARCHAR,
    curso VARCHAR,
    bairro VARCHAR,
    cidade VARCHAR,
    estado VARCHAR,
    situacao VARCHAR,
    escfinal REAL,
    polo VARCHAR,
    origem VARCHAR,
    etnia VARCHAR,
    catinscr VARCHAR,
    catselec VARCHAR,
    estcivil VARCHAR,
    numfilho VARCHAR,
    resid VARCHAR,
    tipo1gr VARCHAR,
    turn1gr VARCHAR,
    nat1gr VARCHAR,
    tipo2gr VARCHAR,
    loc2gr VARCHAR,
    turn2gr VARCHAR,
    nat2gr VARCHAR,
    ano2gr VARCHAR,
    tipcurso VARCHAR,
    numvest VARCHAR,
    cursinho VARCHAR,
    influenc VARCHAR,
    expectat VARCHAR,
    trab VARCHAR,
    vidaecon VARCHAR,
    cargahor VARCHAR,
    tipocup VARCHAR,
    prettrab VARCHAR,
    rendatot VARCHAR,
    resprend VARCHAR,
    instrpai VARCHAR,
    instrmae VARCHAR,
    trabconj VARCHAR,
    ocuppai VARCHAR,
    ocupmae VARCHAR,
    celular VARCHAR,
    comput VARCHAR,
    internet VARCHAR,
    quarto VARCHAR,
    automovel VARCHAR,
    cor VARCHAR,
    PRIMARY KEY(ano, inscrica, area, curso)
);


CREATE TABLE IF NOT EXISTS ufba_bronze.vestibular_2006_inscritos (
    ano INTEGER,
    inscrica VARCHAR,
    faixaet VARCHAR,
    idade INTEGER,
    nascim VARCHAR,
    sexo VARCHAR,
    deficie VARCHAR,
    area VARCHAR,
    curso VARCHAR,
    bairro VARCHAR,
    cidade VARCHAR,
    estado VARCHAR,
    situacao VARCHAR,
    escfinal REAL,
    polo VARCHAR,
    origem VARCHAR,
    etnia VARCHAR,
    catinscr VARCHAR,
    catselec VARCHAR,
    estcivil VARCHAR,
    numfilho VARCHAR,
    resid VARCHAR,
    tipo1gr VARCHAR,
    turn1gr VARCHAR,
    nat1gr VARCHAR,
    tipo2gr VARCHAR,
    loc2gr VARCHAR,
    turn2gr VARCHAR,
    nat2gr VARCHAR,
    ano2gr VARCHAR,
    tipcurso VARCHAR,
    numvest VARCHAR,
    cursinho VARCHAR,
    influenc VARCHAR,
    expectat VARCHAR,
    trab VARCHAR,
    vidaecon VARCHAR,
    cargahor VARCHAR,
    tipocup VARCHAR,
    prettrab VARCHAR,
    rendatot VARCHAR,
    resprend VARCHAR,
    instrpai VARCHAR,
    instrmae VARCHAR,
    trabconj VARCHAR,
    ocuppai VARCHAR,
    ocupmae VARCHAR,
    celular VARCHAR,
    comput VARCHAR,
    internet VARCHAR,
    quarto VARCHAR,
    carro VARCHAR,
    cor VARCHAR,
    cor2 VARCHAR,
    PRIMARY KEY(ano, inscrica, area, curso)
);


CREATE TABLE IF NOT EXISTS ufba_bronze.vestibular_2007_inscritos (
    ano INTEGER,
    inscrica VARCHAR,
    faixaet VARCHAR,
    idade INTEGER,
    nascim VARCHAR,
    sexo VARCHAR,
    deficie VARCHAR,
    area VARCHAR,
    curso VARCHAR,
    bairro VARCHAR,
    cidade VARCHAR,
    estado VARCHAR,
    situacao VARCHAR,
    escfinal REAL,
    polo VARCHAR,
    origem VARCHAR,
    etnia VARCHAR,
    catselec VARCHAR,
    estcivil VARCHAR,
    numfilho VARCHAR,
    resid VARCHAR,
    tipo1gr VARCHAR,
    turn1gr VARCHAR,
    nat1gr VARCHAR,
    tipo2gr VARCHAR,
    loc2gr VARCHAR,
    turn2gr VARCHAR,
    nat2gr VARCHAR,
    ano2gr VARCHAR,
    tipcurso VARCHAR,
    numvest VARCHAR,
    cursinho VARCHAR,
    influenc VARCHAR,
    expectat VARCHAR,
    trab VARCHAR,
    vidaecon VARCHAR,
    cargahor VARCHAR,
    tipocup VARCHAR,
    prettrab VARCHAR,
    rendatot VARCHAR,
    resprend VARCHAR,
    instrpai VARCHAR,
    instrmae VARCHAR,
    trabconj VARCHAR,
    ocuppai VARCHAR,
    ocupmae VARCHAR,
    celular VARCHAR,
    comput VARCHAR,
    internet VARCHAR,
    quarto VARCHAR,
    automove VARCHAR,
    cor VARCHAR,
    PRIMARY KEY(ano, inscrica, area, curso)
);


CREATE TABLE IF NOT EXISTS ufba_bronze.vestibular_2008_inscritos (
    ano INTEGER,
    inscrica VARCHAR,
    nascim VARCHAR,
    idade INTEGER,
    faixaet VARCHAR,
    sexo VARCHAR,
    deficie VARCHAR,
    area VARCHAR,
    curso VARCHAR,
    bairro VARCHAR,
    cidade VARCHAR,
    estado VARCHAR,
    situacao VARCHAR,
    escfinal REAL,
    polo VARCHAR,
    origem VARCHAR,
    etnia VARCHAR,
    catselec VARCHAR,
    estcivil VARCHAR,
    numfilho VARCHAR,
    resid VARCHAR,
    tipo1gr VARCHAR,
    turn1gr VARCHAR,
    nat1gr VARCHAR,
    tipo2gr VARCHAR,
    loc2gr VARCHAR,
    turn2gr VARCHAR,
    nat2gr VARCHAR,
    ano2gr VARCHAR,
    tipcurso VARCHAR,
    numvest VARCHAR,
    cursinho VARCHAR,
    influenc VARCHAR,
    expectat VARCHAR,
    trab VARCHAR,
    vidaecon VARCHAR,
    cargahor VARCHAR,
    tipocup VARCHAR,
    prettrab VARCHAR,
    rendatot VARCHAR,
    resprend VARCHAR,
    instrpai VARCHAR,
    instrmae VARCHAR,
    trabconj VARCHAR,
    ocuppai VARCHAR,
    ocupmae VARCHAR,
    celular VARCHAR,
    comput VARCHAR,
    internet VARCHAR,
    quarto VARCHAR,
    automove VARCHAR,
    cor VARCHAR,
    PRIMARY KEY(ano, inscrica, area, curso)
);


CREATE TABLE IF NOT EXISTS ufba_bronze.vestibular_2009_inscritos (
    ano INTEGER,
    inscrica VARCHAR,
    dt_nasci VARCHAR,
    idade INTEGER,
    faixaet VARCHAR,
    sexo VARCHAR,
    deficie VARCHAR,
    area VARCHAR,
    curso VARCHAR,
    bairro VARCHAR,
    cidade VARCHAR,
    estado VARCHAR,
    situacao VARCHAR,
    escfinal REAL,
    polo VARCHAR,
    origem VARCHAR,
    etnia VARCHAR,
    catselec VARCHAR,
    estcivil VARCHAR,
    numfilho VARCHAR,
    resid VARCHAR,
    tipo1gr VARCHAR,
    turn1gr VARCHAR,
    nat1gr VARCHAR,
    tipo2gr VARCHAR,
    loc2gr VARCHAR,
    turn2gr VARCHAR,
    nat2gr VARCHAR,
    ano2gr VARCHAR,
    tipcurso VARCHAR,
    numvest VARCHAR,
    cursinho VARCHAR,
    influenc VARCHAR,
    expectat VARCHAR,
    trab VARCHAR,
    vidaecon VARCHAR,
    cargahor VARCHAR,
    tipocup VARCHAR,
    prettrab VARCHAR,
    rendatot VARCHAR,
    resprend VARCHAR,
    instrpai VARCHAR,
    instrmae VARCHAR,
    trabconj VARCHAR,
    ocuppai VARCHAR,
    ocupmae VARCHAR,
    celular VARCHAR,
    comput VARCHAR,
    internet VARCHAR,
    quarto VARCHAR,
    automove VARCHAR,
    cor VARCHAR,
    cor2 VARCHAR,
    PRIMARY KEY(ano, inscrica, area, curso)
);



CREATE TABLE IF NOT EXISTS ufba_bronze.bi_vestibular_2009_inscritos (
    ano INTEGER,
    inscrica VARCHAR,
    dt_nasci VARCHAR,
    idade INTEGER,
    faixaet VARCHAR,
    sexo VARCHAR,
    deficie VARCHAR,
    area VARCHAR,
    curso VARCHAR,
    bairro VARCHAR,
    cidade VARCHAR,
    estado VARCHAR,
    situacao VARCHAR,
    escfinal REAL,
    polo VARCHAR,
    origem VARCHAR,
    etnia VARCHAR,
    catselec VARCHAR,
    estcivil VARCHAR,
    numfilho VARCHAR,
    resid VARCHAR,
    tipo1gr VARCHAR,
    turn1gr VARCHAR,
    nat1gr VARCHAR,
    tipo2gr VARCHAR,
    loc2gr VARCHAR,
    turn2gr VARCHAR,
    nat2gr VARCHAR,
    ano2gr VARCHAR,
    tipcurso VARCHAR,
    numvest VARCHAR,
    cursinho VARCHAR,
    influenc VARCHAR,
    expectat VARCHAR,
    trab VARCHAR,
    vidaecon VARCHAR,
    cargahor VARCHAR,
    tipocup VARCHAR,
    prettrab VARCHAR,
    rendatot VARCHAR,
    resprend VARCHAR,
    instrpai VARCHAR,
    instrmae VARCHAR,
    trabconj VARCHAR,
    ocuppai VARCHAR,
    ocupmae VARCHAR,
    celular VARCHAR,
    comput VARCHAR,
    internet VARCHAR,
    quarto VARCHAR,
    automove VARCHAR,
    cor VARCHAR,
    cor2 VARCHAR,
    PRIMARY KEY(ano, inscrica, area, curso)
);


CREATE TABLE IF NOT EXISTS ufba_bronze.vestibular_2010_inscritos (
    ano INTEGER,
    inscrica VARCHAR,
    dt_nasci VARCHAR,
    idade INTEGER,
    faixaet VARCHAR,
    sexo VARCHAR,
    deficie VARCHAR,
    area VARCHAR,
    curso VARCHAR,
    bairro VARCHAR,
    cidade VARCHAR,
    estado VARCHAR,
    situacao VARCHAR,
    escfinal REAL,
    polo VARCHAR,
    origem VARCHAR,
    etnia VARCHAR,
    catselec VARCHAR,
    cpf VARCHAR,
    estcivil VARCHAR,
    numfilho VARCHAR,
    resid VARCHAR,
    tipo1gr VARCHAR,
    turn1gr VARCHAR,
    nat1gr VARCHAR,
    tipo2gr VARCHAR,
    loc2gr VARCHAR,
    turn2gr VARCHAR,
    nat2gr VARCHAR,
    ano2gr VARCHAR,
    tipcurso VARCHAR,
    numvest VARCHAR,
    cursinho VARCHAR,
    influenc VARCHAR,
    expectat VARCHAR,
    trab VARCHAR,
    vidaecon VARCHAR,
    cargahor VARCHAR,
    tipocup VARCHAR,
    prettrab VARCHAR,
    rendatot VARCHAR,
    resprend VARCHAR,
    instrpai VARCHAR,
    instrmae VARCHAR,
    trabconj VARCHAR,
    ocuppai VARCHAR,
    ocupmae VARCHAR,
    celular VARCHAR,
    comput VARCHAR,
    internet VARCHAR,
    quarto VARCHAR,
    automove VARCHAR,
    cor VARCHAR,
    cor2 VARCHAR,
    PRIMARY KEY(ano, inscrica, cpf, area, curso)
);


CREATE TABLE IF NOT EXISTS ufba_bronze.bi_vestibular_2010_inscritos (
    ano INTEGER,
    inscrica VARCHAR,
    dt_nascim VARCHAR,
    idade INTEGER,
    faixaet VARCHAR,
    sexo VARCHAR,
    deficie VARCHAR,
    area VARCHAR,
    curso VARCHAR,
    bairro VARCHAR,
    cidade VARCHAR,
    estado VARCHAR,
    situacao VARCHAR,
    escfinal REAL,
    polo VARCHAR,
    origem VARCHAR,
    etnia VARCHAR,
    catselec VARCHAR,
    cpf VARCHAR,
    estcivil VARCHAR,
    numfilho VARCHAR,
    resid VARCHAR,
    tipo1gr VARCHAR,
    turn1gr VARCHAR,
    nat1gr VARCHAR,
    tipo2gr VARCHAR,
    loc2gr VARCHAR,
    turn2gr VARCHAR,
    nat2gr VARCHAR,
    ano2gr VARCHAR,
    tipcurso VARCHAR,
    numvest VARCHAR,
    cursinho VARCHAR,
    influenc VARCHAR,
    expectat VARCHAR,
    trab VARCHAR,
    vidaecon VARCHAR,
    cargahor VARCHAR,
    tipocup VARCHAR,
    prettrab VARCHAR,
    rendatot VARCHAR,
    resprend VARCHAR,
    instrpai VARCHAR,
    instrmae VARCHAR,
    trabconj VARCHAR,
    ocuppai VARCHAR,
    ocupmae VARCHAR,
    celular VARCHAR,
    comput VARCHAR,
    internet VARCHAR,
    quarto VARCHAR,
    automove VARCHAR,
    cor VARCHAR,
    PRIMARY KEY(ano, inscrica, cpf, area, curso)
);


CREATE TABLE IF NOT EXISTS ufba_bronze.vestibular_2011_inscritos (
    ano INTEGER,
    inscrica VARCHAR,
    dt_nasci VARCHAR,
    idade INTEGER,
    faixaet VARCHAR,
    sexo VARCHAR,
    deficie VARCHAR,
    area VARCHAR,
    curso VARCHAR,
    bairro VARCHAR,
    cidade VARCHAR,
    estado VARCHAR,
    situacao VARCHAR,
    escfinal REAL,
    polo VARCHAR,
    origem VARCHAR,
    etnia VARCHAR,
    catselec VARCHAR,
    cpf VARCHAR,
    estcivil VARCHAR,
    numfilho VARCHAR,
    resid VARCHAR,
    tipo1gr VARCHAR,
    turn1gr VARCHAR,
    nat1gr VARCHAR,
    tipo2gr VARCHAR,
    loc2gr VARCHAR,
    turn2gr VARCHAR,
    nat2gr VARCHAR,
    ano2gr VARCHAR,
    tipcurso VARCHAR,
    numvest VARCHAR,
    cursinho VARCHAR,
    influenc VARCHAR,
    expectat VARCHAR,
    trab VARCHAR,
    vidaecon VARCHAR,
    cargahor VARCHAR,
    tipocup VARCHAR,
    prettrab VARCHAR,
    rendatot VARCHAR,
    resprend VARCHAR,
    instrpai VARCHAR,
    instrmae VARCHAR,
    trabconj VARCHAR,
    ocuppai VARCHAR,
    ocupmae VARCHAR,
    celular VARCHAR,
    comput VARCHAR,
    internet VARCHAR,
    quarto VARCHAR,
    automove VARCHAR,
    cor VARCHAR,
    cor2 VARCHAR,
    PRIMARY KEY(ano, inscrica, cpf, area, curso)
);


CREATE TABLE IF NOT EXISTS ufba_bronze.bi_vestibular_2011_inscritos (
    ano INTEGER,
    inscrica VARCHAR,
    nascim VARCHAR,
    idade INTEGER,
    faixaet VARCHAR,
    sexo VARCHAR,
    deficie VARCHAR,
    area VARCHAR,
    curso VARCHAR,
    bairro VARCHAR,
    cidade VARCHAR,
    estado VARCHAR,
    situacao VARCHAR,
    escfinal REAL,
    polo VARCHAR,
    origem VARCHAR,
    etnia VARCHAR,
    catselec VARCHAR,
    cpf VARCHAR,
    estcivil VARCHAR,
    numfilho VARCHAR,
    resid VARCHAR,
    tipo1gr VARCHAR,
    turn1gr VARCHAR,
    nat1gr VARCHAR,
    tipo2gr VARCHAR,
    loc2gr VARCHAR,
    turn2gr VARCHAR,
    nat2gr VARCHAR,
    ano2gr VARCHAR,
    tipcurso VARCHAR,
    numvest VARCHAR,
    cursinho VARCHAR,
    influenc VARCHAR,
    expectat VARCHAR,
    trab VARCHAR,
    vidaecon VARCHAR,
    cargahor VARCHAR,
    tipocup VARCHAR,
    prettrab VARCHAR,
    rendatot VARCHAR,
    resprend VARCHAR,
    instrpai VARCHAR,
    instrmae VARCHAR,
    trabconj VARCHAR,
    ocuppai VARCHAR,
    ocupmae VARCHAR,
    celular VARCHAR,
    comput VARCHAR,
    internet VARCHAR,
    quarto VARCHAR,
    automove VARCHAR,
    cor VARCHAR,
    PRIMARY KEY(ano, inscrica, cpf, area, curso)
);


CREATE TABLE IF NOT EXISTS ufba_bronze.vestibular_2012_inscritos (
    ano INTEGER,
    inscrica VARCHAR,
    nascim VARCHAR,
    idade INTEGER,
    faixaet VARCHAR,
    sexo VARCHAR,
    deficie VARCHAR,
    area VARCHAR,
    curso VARCHAR,
    bairro VARCHAR,
    cidade VARCHAR,
    estado VARCHAR,
    situacao VARCHAR,
    escfinal REAL,
    polo VARCHAR,
    origem VARCHAR,
    etnia VARCHAR,
    catselec VARCHAR,
    cpf VARCHAR,
    estcivil VARCHAR,
    numfilho VARCHAR,
    resid VARCHAR,
    tipo1gr VARCHAR,
    turn1gr VARCHAR,
    tipo1grconc VARCHAR,
    tipo2gr VARCHAR,
    loc2gr VARCHAR,
    turn2gr VARCHAR,
    tipo2grconc VARCHAR,
    ano2gr VARCHAR,
    tipcurso VARCHAR,
    numvest VARCHAR,
    cursinho VARCHAR,
    influenc VARCHAR,
    expectat VARCHAR,
    trab VARCHAR,
    vidaecon VARCHAR,
    cargahor VARCHAR,
    tipocup VARCHAR,
    prettrab VARCHAR,
    rendatot VARCHAR,
    resprend VARCHAR,
    instrpai VARCHAR,
    instrmae VARCHAR,
    trabconj VARCHAR,
    ocuppai VARCHAR,
    ocupmae VARCHAR,
    celular VARCHAR,
    comput VARCHAR,
    internet VARCHAR,
    quarto VARCHAR,
    automove VARCHAR,
    cor VARCHAR,
    cor2 VARCHAR,
    PRIMARY KEY(ano, inscrica, cpf, area, curso)
);


CREATE TABLE IF NOT EXISTS ufba_bronze.bi_vestibular_2012_inscritos (
    ano INTEGER,
    inscrica VARCHAR,
    nascim VARCHAR,
    idade INTEGER,
    faixaet VARCHAR,
    sexo VARCHAR,
    deficie VARCHAR,
    area VARCHAR,
    curso VARCHAR,
    bairro VARCHAR,
    cidade VARCHAR,
    estado VARCHAR,
    situacao VARCHAR,
    escfinal REAL,
    polo VARCHAR,
    origem VARCHAR,
    etnia VARCHAR,
    catselec VARCHAR,
    cpf VARCHAR,
    estcivil VARCHAR,
    numfilho VARCHAR,
    resid VARCHAR,
    tipo1gr VARCHAR,
    turn1gr VARCHAR,
    tipo2gr VARCHAR,
    loc2gr VARCHAR,
    turn2gr VARCHAR,
    ano2gr VARCHAR,
    tipcurso VARCHAR,
    numvest VARCHAR,
    cursinho VARCHAR,
    influenc VARCHAR,
    expectat VARCHAR,
    trab VARCHAR,
    vidaecon VARCHAR,
    cargahor VARCHAR,
    tipocup VARCHAR,
    prettrab VARCHAR,
    rendatot VARCHAR,
    resprend VARCHAR,
    instrpai VARCHAR,
    instrmae VARCHAR,
    trabconj VARCHAR,
    ocuppai VARCHAR,
    ocupmae VARCHAR,
    celular VARCHAR,
    comput VARCHAR,
    internet VARCHAR,
    quarto VARCHAR,
    automove VARCHAR,
    cor VARCHAR,
    cor2 VARCHAR,
    PRIMARY KEY(ano, inscrica, cpf, area, curso)
);


CREATE TABLE IF NOT EXISTS ufba_bronze.vestibular_2013_inscritos (
    ano INTEGER,
    inscrica VARCHAR,
    nascim VARCHAR,
    idade INTEGER,
    faixaet VARCHAR,
    sexo VARCHAR,
    deficie VARCHAR,
    area VARCHAR,
    curso VARCHAR,
    bairro VARCHAR,
    cidade VARCHAR,
    estado VARCHAR,
    situacao VARCHAR,
    escfinal REAL,
    polo VARCHAR,
    origem VARCHAR,
    etnia VARCHAR,
    catselec VARCHAR,
    cpf VARCHAR,
    estcivil VARCHAR,
    numfilho VARCHAR,
    resid VARCHAR,
    tipo1gr VARCHAR,
    turn1gr VARCHAR,
    tipo1grconc VARCHAR,
    tipo2gr VARCHAR,
    loc2gr VARCHAR,
    turn2gr VARCHAR,
    tipo2grconc VARCHAR,
    ano2gr VARCHAR,
    tipcurso VARCHAR,
    numvest VARCHAR,
    cursinho VARCHAR,
    influenc VARCHAR,
    expectat VARCHAR,
    trab VARCHAR,
    vidaecon VARCHAR,
    cargahor VARCHAR,
    tipocup VARCHAR,
    prettrab VARCHAR,
    rendatot VARCHAR,
    resprend VARCHAR,
    instrpai VARCHAR,
    instrmae VARCHAR,
    trabconj VARCHAR,
    ocuppai VARCHAR,
    ocupmae VARCHAR,
    celular VARCHAR,
    comput VARCHAR,
    internet VARCHAR,
    quarto VARCHAR,
    automove VARCHAR,
    cor VARCHAR,
    PRIMARY KEY(ano, inscrica, cpf, area, curso)
);


CREATE TABLE IF NOT EXISTS ufba_bronze.bi_vestibular_2013_inscritos (
    ano INTEGER,
    inscrica VARCHAR,
    nascim VARCHAR,
    idade INTEGER,
    faixaet VARCHAR,
    sexo VARCHAR,
    deficie VARCHAR,
    area VARCHAR,
    curso VARCHAR,
    bairro VARCHAR,
    cidade VARCHAR,
    estado VARCHAR,
    situacao VARCHAR,
    escfinal REAL,
    polo VARCHAR,
    origem VARCHAR,
    etnia VARCHAR,
    catselec VARCHAR,
    cpf VARCHAR,
    estcivil VARCHAR,
    numfilho VARCHAR,
    resid VARCHAR,
    tipo1gr VARCHAR,
    turn1gr VARCHAR,
    tipo1grconc VARCHAR,
    tipo2gr VARCHAR,
    loc2gr VARCHAR,
    turn2gr VARCHAR,
    tipo2grconc VARCHAR,
    ano2gr VARCHAR,
    tipcurso VARCHAR,
    numvest VARCHAR,
    cursinho VARCHAR,
    influenc VARCHAR,
    expectat VARCHAR,
    trab VARCHAR,
    vidaecon VARCHAR,
    cargahor VARCHAR,
    tipocup VARCHAR,
    prettrab VARCHAR,
    rendatot VARCHAR,
    resprend VARCHAR,
    instrpai VARCHAR,
    instrmae VARCHAR,
    trabconj VARCHAR,
    ocuppai VARCHAR,
    ocupmae VARCHAR,
    celular VARCHAR,
    comput VARCHAR,
    internet VARCHAR,
    quarto VARCHAR,
    automove VARCHAR,
    cor VARCHAR,
    PRIMARY KEY(ano, inscrica, cpf, area, curso)
);


CREATE TABLE IF NOT EXISTS ufba_bronze.ufba_academica_0321 (
    cpf VARCHAR,
    mtr VARCHAR,
    inscrica VARCHAR,
    nome VARCHAR,
    per_ingr INTEGER,
    cd_forma_ingr INTEGER,
    descr_forma_ingr VARCHAR,
    per_saida INTEGER,
    cd_forma_saida INTEGER,
    descr_forma_saida VARCHAR,
    cr REAL,
    escore REAL,
    class_geral INTEGER,
    categoria_class VARCHAR,
    cod_curso INTEGER,
    per_crs_ini INTEGER,
    nome_curso VARCHAR,
    colegiado INTEGER,
    col_nm_colegiado VARCHAR,
    per_let_disc INTEGER,
    disc VARCHAR,
    ch_disc REAL,
    nat_disc VARCHAR,
    tur INTEGER,
    nota REAL,
    resultado VARCHAR,
    doc_nu_matricula_docente VARCHAR,
    doc_nm_docente VARCHAR,
    doc_vinculo VARCHAR,
    doc_titulacao VARCHAR,
    doc_nivel VARCHAR,
    doc_regime_trab VARCHAR,
    nascimento VARCHAR,
    aln_cd_estado_civil INTEGER,
    ecv_ds_estado_civil VARCHAR,
    sexo VARCHAR,
    dtnasc VARCHAR,
    aln_sg_estado_nascimento VARCHAR,
    aln_nm_pai VARCHAR,
    aln_nm_mae VARCHAR,
    aln_cd_cor INTEGER,
    cor_nm_cor VARCHAR,
    aln_nm_cidade_nascimento VARCHAR,
    eda_nm_email VARCHAR,
    pk_id_academic VARCHAR PRIMARY KEY
)